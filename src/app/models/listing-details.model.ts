/** Placeholder listing facts shown in the closing call-to-action section. */
export interface ListingDetails {
  readonly price: string;
  readonly bedroomCount: string;
  readonly bathroomCount: string;
  readonly squareFootage: string;
  readonly listingUrl: string;
}
